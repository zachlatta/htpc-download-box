# Your timezone, https://en.wikipedia.org/wiki/List_of_tz_database_time_zones
TZ=America/New_York
# UNIX PUID and PGID, find with: id $USER
PUID=1000
PGID=1000
# The directory where data and configuration will be stored.
ROOT=/media
# There is a shared directory mounted in the VM at /media. SQLite doesn't like
# running over the shared directory, so /media-edge-cases exists on the local
# FS for things that need it.
ROOT_EDGE=/media-edge-cases
